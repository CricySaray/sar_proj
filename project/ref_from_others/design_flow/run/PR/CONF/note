U001: A separate function for loading variable names should implement as many isolation operations as possible, which can expand the room for subsequent functional extensions.
U002: Add the operation of merging variable definition files into a single file to facilitate querying and searching.
U003: Create a proc that can take a variable name as input and return the level it is at.
