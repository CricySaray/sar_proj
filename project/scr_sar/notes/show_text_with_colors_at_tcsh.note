========================================================================
tcsh shell
探索结论：
tcsh 内置 echo 对转义字符支持不稳定，优先使用 printf 实现彩色文字，兼容性最好。
语法模板：
printf "\033[样式编码 m 输出文本 \033 [0m\n"

核心说明：
\033       ANSI 转义起始符，tcsh 环境不要使用 \e，优先固定写 \033
[编码 m     颜色 / 样式控制段
\033 [0m    重置所有颜色样式，【必须结尾带上】，否则终端后续文字持续变色

==================== 前景色（文字颜色）编码表 ====================
编码  颜色名称
30    黑色
31    红色
32    绿色
33    黄色
34    蓝色
35    紫色 (品红)
36    青色
37    白色

==================== 背景色编码表 ====================
编码  颜色名称
40    黑色背景
41    红色背景
42    绿色背景
43    黄色背景
44    蓝色背景
45    紫色背景
46    青色背景
47    白色背景

==================== 通用样式控制编码 ====================
编码  效果
0     重置所有样式（\033 [0m）
1     加粗高亮
4     下划线

多个样式组合：使用分号；分隔编码
示例：1;32 代表 加粗 + 绿色文字

==================== 可用示例代码（直接复制测试） ====================

# 普通红色文字

printf "\033 [31m 红色文字 \033 [0m\n"

# 加粗绿色

printf "\033 [1;32m 加粗绿色文字 \033 [0m\n"

# 黄色文字 + 蓝色背景

printf "\033 [33;44m 黄字蓝底 \033 [0m\n"

# 青色 + 下划线

printf "\033 [4;36m 青色带下划线文字 \033 [0m\n"

========================================================================
重要知识点 & 踩坑清单（重点记忆）

1. tcsh 内置 echo 缺陷：
原生 echo 对 \033 转义解析不稳定；不推荐直接使用 echo。
若坚持用 echo，需要调用外部 /bin/echo -e，不推荐脚本内使用。
2. 引号规则：
printf 字符串使用双引号；单引号内不会解析 \ 转义符号。
3. 强制规范：
每一条彩色输出末尾务必追加 \033 [0m，防止终端颜色污染。
4. 字符写法：
不要使用 \e，tcsh 环境统一使用八进制 \033，兼容性更广。
5. Tmux 环境额外注意：
颜色异常时，确认环境变量 setenv TERM xterm-256color
6. 组合格式顺序无强制要求：
\033 [1;32m 等价于 \033 [32;1m
========================================================================


我自己测试的案例：
printf "\033[1;4;31;46m this is a test line.\033[0m\n"
## 上面是加粗、下划线、红色字体以及青色背景色的显示
