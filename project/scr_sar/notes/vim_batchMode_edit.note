=============================================================================
VIM 无UI(无头)批量处理笔记
=============================================================================

# 核心概念
无头批量：不打开vim交互界面，后台执行Vim Ex命令处理文件。
仅可使用 Ex 命令(即vim中 : 之后的命令)。
禁止普通模式按键、可视模式(v/V)操作，该类操作在无头环境行为不可预测。

# 命令参数说明(保证跨版本稳定，Vim7/Vim8通用)
vim -d -n [--headless] -c "EX_CMD" -c "EX_CMD" -c "wq" target_file

参数：
  -d         不加载 ~/.vimrc、插件、自定义映射，环境干净，避免本地配置干扰
  -n         禁用 .swp 交换文件。批量处理必加，防止E325交换文件冲突卡死脚本
  --headless  Vim8.1+ 无头模式，完全不拉起终端UI；Vim7无此参数，直接删掉
  -c "cmd"   执行Ex命令，可多次叠加，顺序执行
  -S script  从文件加载一批Ex命令，适合复杂逻辑
  wq         保存退出；q! 不保存强制退出

# 两种使用方式
## 1.命令行直接传入简短命令
vim -d -n --headless -c "g/^$/d" -c "%s/a/b/ge" -c "wq" file.rpt

## 2.外部脚本文件(推荐复杂逻辑)
# process.vim 内容，只写Ex命令，不要加冒号，不要写普通模式按键
g/^$/d
%s/\s\+$//ge
wq

# 执行
vim -d -n --headless -S process.vim file.rpt

# shell批量循环处理一类文件
for f in *.rpt; do
  vim -d -n --headless -S process.vim "$f"
done

# Vim7兼容写法，直接去掉--headless
vim -d -n -S process.vim file.rpt

# 备选：ex -s (老旧Unix环境，ex是vim底层)
ex -s file <<'EOF'
g/^$/d
%s/a/b/ge
wq
EOF

# Ex常用命令速查(交互操作 → 批量Ex写法)
交互手动操作          批量Ex命令
ggdG(清空全部)        %d
删除空行              g/^$/d
行尾去空格            %s/\s\+$//ge
全局替换              %s/old/new/ge
删除匹配行            g/PATTERN/d
保留匹配行删除其余    v/PATTERN/d
复制全部到寄存器A     1,$y A
寄存器A粘贴到文件尾   $put a
匹配行移到文件末尾    g/PATTERN/m$

=============================================================================
# 【重点踩坑注意事项，务必遵守】
=============================================================================
1. 模式限制
   批量环境只信任Ex命令。不要使用普通模式快捷键 j/k/dd/gg/G，不要使用可视v/V选中。
   普通/可视模式逻辑在无头环境行为不稳定，会产生错误结果或卡死。

2. 必须带上 -d -n
   -d：跳过用户vimrc与插件，不同服务器运行结果一致，不受个人配置影响。
   -n：关闭swap交换文件。批量并发处理极易生成.swp，触发E325提示，脚本直接卡住。

3. 防止交互阻塞(脚本卡死头号原因)
   任何会弹出提示、等待回车的操作都会卡住无头vim。
   - 替换命令后缀加 e：%s/pat/sub/ge ，无匹配不抛出错误，不会进入交互等待。
   - 文件只读场景：w会报错，提前shell判断权限，或使用w!强制写入。
   - 避免 :find / :edit 打开不存在文件。

4. 退出码不可靠
   vim无头执行出错，shell $?不一定返回非0。不要只依靠退出码判断处理成功。
   建议：处理完成后用shell简单校验，例如grep检查关键字、判断文件大小。

5. shell循环处理文件
   文件变量必须双引号包裹 "$f"，兼容文件名含空格、特殊字符。

6. Vim版本差异
   Vim7：无--headless参数，删除该参数，其余参数保留可用，会短暂渲染终端，不影响文件输出。
   Neovim：使用 nvim --headless，参数逻辑与vim保持一致。

7. 寄存器与剪贴板
   无头环境不会继承交互会话寄存器内容。系统剪贴板寄存器 * + 不一定初始化。
   脚本内部操作优先使用vim命名寄存器 a‑z。

8. 禁用的功能
   无头模式不加载语法高亮、代码折叠相关逻辑，不要在脚本中依赖这类功能，文本修改不受影响。

9. 退出命令选择
   修改文件后保存退出：wq
   不改动直接丢弃退出：q!
   强制保存退出：wq!

10. 选型提醒
    如果sed/awk可以实现，优先sed/awk。
    仅当需要vim特有的正则、g全局命令、寄存器、复杂行逻辑时，再选用vim无头批量。

