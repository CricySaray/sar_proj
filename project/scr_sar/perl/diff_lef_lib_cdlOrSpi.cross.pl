#!/bin/perl
# --------------------------
# author    : sar song
# date      : 2025/10/03 01:33:18 Friday
# label     : getInfo_sub
#   tcl  -> (atomic_proc|display_proc|gui_proc|task_proc|dump_proc|check_proc|math_proc|package_proc|test_proc|datatype_proc|db_proc|flow_proc|report_proc|cross_lang_proc|misc_proc)
#   perl -> (format_sub|getInfo_sub|perl_task)
# descrip   : By inputting lib, lef, cdl, or spi files and then specifying the name of the celltype to be compared, the system will automatically extract their port information 
#             from these types of files, compare whether the number of port names and the port names themselves match, and then output a comparison result file. This is used to 
#             quickly check if the ports in the lib, lef, and cdl files correspond to each other.
# return    : diff output file
# ref       : link url
# --------------------------
# TO_WRITE
